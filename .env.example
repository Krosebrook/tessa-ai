# Tessa AI Environment Variables
# Copy this file to .env and fill in your values

# Base44 Configuration (Required)
# Get your App ID from https://base44.com
VITE_BASE44_APP_ID=your_app_id_here

# Base44 Functions Version (Required)
# Use 'prod' for production, 'dev' for development
VITE_BASE44_FUNCTIONS_VERSION=prod

# Development Options (Optional)
# Enable legacy SDK imports if needed
# BASE44_LEGACY_SDK_IMPORTS=false

# Notes:
# - Only variables prefixed with VITE_ are exposed to the client
# - Never commit .env files to version control
# - Keep sensitive data out of VITE_ variables (they're public in the bundle)
